<Window x:Class="TouchGrass.Views.AddGameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Game" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Game Title" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Text="Cover Image" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Border BorderBrush="Gray" BorderThickness="1" Background="#EEE" HorizontalAlignment="Center">
                <Image x:Name="CoverImagePreview" Stretch="Uniform" Width="150" Height="200"/>
            </Border>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Margin="0,0,10,0" Padding="15,5"/>
            <Button Content="Save" Click="Save_Click" IsDefault="True" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>
