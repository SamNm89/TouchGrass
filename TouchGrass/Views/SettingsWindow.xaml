<Window x:Class="TouchGrass.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Hotkey Section -->
        <GroupBox Header="Global Hotkey" Grid.Row="0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="HotkeyTextBox" IsReadOnly="True" PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" 
                         Text="Alt + Space" VerticalContentAlignment="Center" Height="25"/>
                <TextBlock Grid.Column="1" Text="Press any key combination" 
                           VerticalAlignment="Center" Margin="10,0,0,0" FontStyle="Italic" Foreground="Gray"/>
            </Grid>
        </GroupBox>

        <!-- Startup Section -->
        <GroupBox Header="General" Grid.Row="1" Margin="0,10,0,0" Padding="10">
            <StackPanel>
                <CheckBox x:Name="RunOnStartupCheckBox" Content="Run on Startup" Margin="0,0,0,5"/>
                <CheckBox x:Name="ShowTrayIconCheckBox" Content="Show Tray Icon"/>
            </StackPanel>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="25" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="Save" Width="80" Height="25" Click="Save_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
